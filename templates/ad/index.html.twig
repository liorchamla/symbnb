{% extends 'base.html.twig' %}

{% block title %}Liste des annonces{% endblock %}

{% block body %}
<section class="ads py-5">
    <div class="container">
        <div class="row">
            {% for ad in ads %}
                {% include 'partials/ad.html.twig' %}
            {% endfor %}
        </div>

        {% set route = 'ads_index' %}
        {% set routeParams = {} %}
        {% include 'partials/pagination.html.twig' %}
    </div>
</section>
{% endblock %}
