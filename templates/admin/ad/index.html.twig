{% extends 'admin/base.html.twig' %}

{% block body %}
    <section class="admin-ads">
        <div class="container-fluid">
            <h1 class="my-3">Gestion des annonces</h1>
            <table class="table table-hover">
                <thead>
                    <tr>
                        <th class="d-none d-md-block">Id</th>
                        <th>Titre</th>
                        <th>Auteur</th>
                        <th class="d-none d-md-block">Réservations</th>
                        <th class="d-none d-md-block">Note</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for ad in ads %}
                        <tr>
                            <td class="d-none d-md-block">{{ad.id}}</td>
                            <td>{{ad.title}}</td>
                            <td><img src="{{ad.owner.picture}}" class="avatar avatar-micro mr-1"> {{ad.owner.firstName}} {{ad.owner.lastName}}</td>
                            <td class="text-center d-none d-md-block"><span class="badge badge-primary rounded">{{ad.bookings|length}}</span></td>
                            <td class="text-center d-none d-md-block"><span class="badge badge-primary rounded">{{ad.avgRatings|number_format(1, ',', ' ')}}</span></td>
                            <td></td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </section>
{% endblock %}
