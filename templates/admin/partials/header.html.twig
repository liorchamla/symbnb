<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="{{ path('home') }}">SymBnB !</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('home') }}">
          <i class="fas fa-chart-line"></i> Dashboard
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('admin_ads_index') }}">
          <i class="fas fa-hotel"></i> Annonces
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('admin_ads_index') }}">
          <i class="fas fa-folder"></i> Réservations
        </a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('admin_comments_index') }}">
          <i class="fas fa-comments"></i> Commentaires
        </a>
      </li>
    </ul>
    <ul class="navbar-nav my-2 my-lg-0">
      {% if app.user %}
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <img src="{{app.user.picture}}" class="avatar avatar-mini mr-1" alt=""> {{app.user.firstName}} {{app.user.lastName}}
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
          <a class="dropdown-item" href="{{path("ads_create")}}">Créer une annonce</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{path("account_index")}}">Mon compte</a>
          <a class="dropdown-item" href="{{path("account_bookings")}}">Mes réservations</a>
          <a class="dropdown-item" href="{{path("account_profile")}}">Modifier mon profile</a>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="{{path("account_logout")}}">Déconnexion</a>
        </div>
      </li>
      {% else %}
        <li class="nav-item">
          <a class="nav-link" href="{{ path('account_login') }}">Connexion</a>
        </li>
      {% endif %}
    </ul>
  </div>
</nav>

{% for label, messages in app.flashes %}
  <div class="container-fluid mt-3">
      <div class="alert alert-{{label}} alert-dismissible">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        {% for message in messages %}
            <p>
                {{ message | raw }}
            </p>
        {% endfor %}
      </div>
  </div>
{% endfor %}